id: positive-trigger-002
name: Positive Trigger 2
description: Status interpretation should trigger session operation guidance.
tags:
  - positive-trigger
inputs:
  prompt: "Interpret a get-status result where one node is waiting, another is stale, and dead-letter files exist."
expected:
  should_trigger: true
graders:
  - type: text
    name: contains-keywords
    config:
      contains:
        - "waiting"
        - "stale"
        - "dead-letter"
