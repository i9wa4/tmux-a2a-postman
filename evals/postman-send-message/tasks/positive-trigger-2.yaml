id: positive-trigger-002
name: Positive Trigger 2
description: Initial-contact request should trigger the send skill.
tags:
  - positive-trigger
inputs:
  prompt: "I need to contact another agent node for the first time and preserve a Markdown body exactly."
expected:
  should_trigger: true
graders:
  - type: text
    name: contains-keywords
    config:
      contains:
        - "send-heredoc"
        - "quoted heredoc"
        - "Markdown"
