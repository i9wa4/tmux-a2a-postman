# PR #647/#648 CI diagnosis

## 1. Evidence

- PR #647 head `527695bd6ac2f3ca324725dc641cd8bb15cf8bb2`, branch
  `issue-619-implement-audit-draw-activation`; PR state was BEHIND,
  MERGEABLE, REVIEW_REQUIRED, with no reviews or comments. Historical run
  `30165305510` failed on duplicate `recordMailboxProjectionPayloadError`.
  The verified head has one declaration, `go test ./internal/daemon/...`
  passes, and golangci-lint reports no finding.
- PR #648 head `4cae4aefacf5bba884bde7945c7bb540d27509a9`, branch
  `issue-620-add-evidence-replay-contract-presence-gate`; PR state was
  BEHIND, MERGEABLE, REVIEW_REQUIRED, with no reviews or comments. Historical
  run `29293497938` failed on unused `parseEnvelopeFromTo` at
  `internal/message/message.go:1070`. The verified head has no such helper and
  `go test ./internal/message/...` passes.
- `gh run watch` concluded both historical runs still FAILURE. Rerun/refresh
  did not produce a fresh authoritative run result in the permitted controls.

## 2. Terminal checklist

- Diagnosis/classification: PASS; failures are stale relative to the verified
  heads and no code delta is justified.
- Publication: BLOCKED by external CI refresh availability and review/merge
  gates; no merge or bypass attempted.
