tmux-a2a-postman — A2A message routing daemon for tmux panes

AI agents send and receive messages. Each message is routed to the recipient
by the daemon. Use send-heredoc to send and pop to read.
Use an explicit command. Bare `tmux-a2a-postman` prints usage; it does not start the daemon.

Quick Start:
  1. Start daemon:    tmux-a2a-postman start
  2. Send a message:
     tmux-a2a-postman send-heredoc --to <node> <<'POSTMAN_BODY'
     message text
     POSTMAN_BODY
  3. Read next msg:   tmux-a2a-postman pop

Key Concepts:
  Node       An AI agent identified by its tmux pane title.
  Edge       A bidirectional routing rule between two nodes (configured in edges).
  Envelope   YAML frontmatter at the top of each message file:
               ---
               params:
                 from: <sender>
                 to: <recipient>
                 timestamp: <ISO 8601>
               ---

Default operator surface:
  send-heredoc               Send a message with an explicit quoted heredoc
  pop                        Read and archive the oldest unread inbox message
  get-status                 Print canonical session health JSON
  get-status-oneline         Print compact all-session health
  inspect-input              Inspect open reply-required work by id
  inspect-message            Inspect persisted message content by id
  version                    Print the build version JSON

Lifecycle and recovery:
  start                      Start the daemon (single-column TUI)
  stop                       Stop the running daemon for this tmux session
  Use `help commands` for the full command list and diagnostic topics.

Messaging Protocol:
  send-heredoc --to <node> <<'POSTMAN_BODY' Send a message from quoted heredoc stdin
  pop                                        Read and archive oldest message
  get-status                                Inspect agent-readable session health
  get-status-oneline                        Inspect compact all-session health
  inspect-input --id <id>                   Inspect an open input request by id
  inspect-message --id <message_id>         Inspect persisted message content by id
  version                                   Print the build version JSON

Topics:
  commands             tmux-a2a-postman help commands
  config               tmux-a2a-postman help config
  directories          tmux-a2a-postman help directories
  messaging            tmux-a2a-postman help messaging
  start                tmux-a2a-postman help start
  stop                 tmux-a2a-postman help stop
  send-heredoc         tmux-a2a-postman help send-heredoc
  send                 tmux-a2a-postman help send
  pop                  tmux-a2a-postman help pop
  get-status           tmux-a2a-postman help get-status
  get-status-oneline   tmux-a2a-postman help get-status-oneline
  inspect-input        tmux-a2a-postman help inspect-input
  inspect-message      tmux-a2a-postman help inspect-message
  version              tmux-a2a-postman help version
  help                 tmux-a2a-postman help help

Run `tmux-a2a-postman help <topic>` for detailed information.
